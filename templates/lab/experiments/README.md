# Experiments

No experiments are implemented yet. Before writing a script, specify the
mathematical objective, hypothesis, parameters, reference, and verification plan.

Follow the root README's reproducibility convention. Keep scripts here; put
generated figures in `../figures/` and run metadata or tables in `../outputs/`.
Add the actual runnable command to the lab README when a script exists.
